# ==============================================================================
# TITAN ICT BOT v14.3 - PRODUCTION REQUIREMENTS
# AUDIT UPGRADE: Added 'python-dotenv' (Critical missing dependency)
# ==============================================================================

# Core Logic & Vectorization (SMC Analyzer)
pandas>=2.1.4
numpy>=1.26

# Messaging & Bridge (Execution)
pyzmq>=25.1.2
httpx>=0.27,<0.29

# Configuration & Persistence
python-dotenv>=1.0.0
pyyaml>=6.0

# tradebot/ (M0 skeleton) config schema — see pyproject.toml for the package's
# own declared dependency; noted here too since this file is the repo's
# single install target (`pip install -r requirements.txt`).
pydantic>=2,<3

# Network & Telemetry
requests>=2.31.0
pytz>=2023.3

# Utilities
colorama>=0.4.6

# Research lake (Parquet storage)
pyarrow>=15.0

# Control GUI API (embedded FastAPI server)
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
# WebSocket backend for uvicorn — WITHOUT this, uvicorn's ws="auto" finds no
# impl and real /ws upgrades fail (the live GUI feed goes dead; only the
# GET /api/state polling fallback survives). The Starlette TestClient masks
# this because it implements WS in-process — a real uvicorn server needs it.
websockets>=12,<16
# Property-based test tier (pass3-systems.md §7.2 names this library; pinning it
# was approved in the S007 spec, 2026-07-27). Test-only and unittest-native —
# @given works on TestCase methods, so no pytest is pulled in. The current
# tests/unit/test_tradebot_properties.py is still seeded-stdlib (no import yet);
# the pin is here so the M1 property tier (P1 needs shrinking) starts from an
# agreed version rather than re-opening the dependency question.
hypothesis>=6,<7
