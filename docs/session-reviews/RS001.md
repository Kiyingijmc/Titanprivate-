# Session Review — S001 · m0-1-tradebot-skeleton-top-level

**Reviewer stance:** independent, adversarial, did not build this work. Audited only the
committed diff `feced8f..f6ac7ca` against `docs/sessions/archive/S001-2026-07-18-m0-1-tradebot-skeleton-top-level.md`
(the session's own scope + DoD), `docs/trading-bot-brainstorm/brainstorm-v2/pass3-systems.md` §2.6,
`docs/trading-bot-brainstorm/brainstorm-v2/pass1-audit.md` (F-034/F-017/F-036), and
`docs/trading-bot-brainstorm/04-architecture-config.md` §B3-B4.

## Environment note (not a session defect)

This review session's sandbox denies **all** code-execution Bash invocations outright —
not just `.venv/bin/python`, but bare `python3 -c "print(1)"` and `python3 -m unittest --help`
were also rejected with "This command requires approval" before any process spawned, with no
prompt surfaced to approve. I confirmed this is a blanket execution gate (not specific to this
package) by dispatching a second, independent subagent to retry the same commands plus isolation
probes (`echo`, `ls`, `python3 --version`) — non-code-executing commands passed, every
code-executing invocation was denied. Separately, this worktree's `.venv` has no `lib/`
(site-packages) at all — only bare `python`/`python3`/`python3.12` symlinks in `bin/` — so
`VERIFY_CMD` could not have run here even if execution were permitted. `.venv/` is gitignored,
so this is worktree/environment state, not something the session committed.

Given this, **I could not run `VERIFY_CMD` and cannot claim to have observed it pass.** In its
place I did a full manual/static trace: hand-evaluated every assertion in
`tests/unit/tradebot/test_config_schema.py` against the literal logic in
`tradebot/config/schema.py` (Pydantic v2 field constraints, validator ordering, the
`compute_binding_order` arithmetic at both the default and the boundary-case position count).
All 19 tests trace to a pass by hand; details below. This should be treated as a lower-confidence
substitute for an actual green `VERIFY_CMD` run, not equivalent to it — a follow-up pass with a
working sandbox/venv should still execute the suite for real.

## Scope discipline

Diff `feced8f..f6ac7ca` touches exactly 7 files:
`pyproject.toml`, `requirements.txt`, `tests/unit/tradebot/__init__.py`,
`tests/unit/tradebot/test_config_schema.py`, `tradebot/__init__.py`,
`tradebot/config/__init__.py`, `tradebot/config/schema.py`.

- No file under `src/`, `config/config.yaml`, or `main.py` touched — matches the DoD's explicit
  scope fence.
- `pyproject.toml` did not exist before this session (verified: `git show feced8f:pyproject.toml`
  → `fatal: path ... does not exist`), so the session's choice of a root-level PEP 621 file (vs.
  a nested `tradebot/pyproject.toml`) is exactly the DoD's documented fallback ("if none exists,
  use a minimal PEP 621 `[project]` table").
- `requirements.txt` gained one line (`pydantic>=2,<3`) plus a comment; no other dependency
  touched — consistent with the spec's "pydantic only, no fastapi/uvicorn/httpx/pyarrow" scope
  fence (those were already present from earlier sessions, untouched here).
- `grep -rn "^import\|^from" tradebot/` shows only stdlib/`pydantic` imports — no `src/` coupling,
  matching the explicit "must not import from `src/`" constraint.
- `tradebot` does not collide with any existing top-level package (`find . -maxdepth 1
  -iname "tradebot*"` → only the new dir).

## DoD checklist — verified by static trace

1. **Package skeleton importable.** `tradebot/__init__.py` and `tradebot/config/__init__.py`
   exist; `tradebot/config/schema.py` has no syntax-level or import-order issues (pure
   stdlib+pydantic imports, `from __future__ import annotations` is compatible with Pydantic v2's
   annotation resolution). Not executed — see environment note.
2. **`pyproject.toml` declares `pydantic`; `requirements.txt` also updated.** Both present
   (`tradebot/config/schema.py:1-30` uses only `pydantic`; `pyproject.toml:10-12`;
   `requirements.txt:21`). Whether `pydantic` is actually importable in *this* `.venv` could not
   be confirmed (empty venv, see above).
3. **F-034 test has teeth.** `test_clamp_has_teeth_against_a_naive_unclamped_formula`
   (`tests/unit/tradebot/test_config_schema.py:40-51`) directly asserts
   `cfg.effective_risk != risk_pct*vol_scalar` at the documented boundary (1.0% × 1.5 = 1.5% vs.
   clamped 1.0%), proving the clamp does real work rather than literally re-running a stripped
   naive implementation. This is a reasonable static proxy for "has teeth" and satisfies DoD
   intent; hand-traced against `PerTradeRiskConfig.effective_risk`
   (`tradebot/config/schema.py:121-126`) — `min(1.0*1.5, 1.0) == 1.0`, matches.
4. **F-017 tests.** Traced `InstrumentConfig._exactly_one_asset_class`
   (`schema.py:276-283`, rejects `len(v) != 1`) and `InstrumentsConfig._validate_membership`
   (`schema.py:314-330`, checks `asset_class` against declared closed set, `correlation_groups`
   subset-of-declared) against all 6 test cases at `test_config_schema.py:71-130` — all trace to
   the expected accept/reject.
5. **F-036 binding-order report.** Hand-computed `compute_binding_order`
   (`schema.py:402-434`) at defaults: `effective_risk=0.5`, `max_positions=12` →
   `ceiling=6.0`; `6.0 < 8.0` (limit #7, account-total) → dead ✓; `6.0 >= {1.5,2.0,4.0,4.0,5.0}`
   (limits #2-#6) → none dead ✓. At `max_positions=16`: `ceiling=8.0`, `8.0 < 8.0` is `False`
   (strict `<`) → limit #7 no longer dead ✓ — matches the boundary-case test at
   `test_config_schema.py:159-166` exactly, including its "no longer strictly under" framing.
   All numeric defaults (`1.5/2.0/4.0/4.0/5.0/8.0/12.0/25.0/600.0/12`) were cross-checked
   line-by-line against `pass3-systems.md:418-430`'s limit table and match exactly, including the
   gross/net counting mode per row (net only for #5 and #6, matching the table's "net (signed)").
6. **Reload-class tagging.** Every `ReloadTaggedModel` subclass field carries a
   `reload_class` via the `reload_field()` wrapper (`schema.py:50-57`, `json_schema_extra`
   round-trips through `reload_classes()`, `schema.py:67-78`); traced the specific asserted
   mappings (`risk`→live, `risk_pct`→live, `instruments`→next-signal) against
   `04-architecture-config.md` §B4's worked examples — match.
7. **No `src/`/`config.yaml`/`main.py` touched** — confirmed above.
8. **Forward-only, path-scoped commit** — confirmed above (single squashed commit,
   7 files, all in-scope).

## Findings

**MINOR** — `CountingMode` declares two enum members (`"n/a"`, `"count"`) that are never
assigned anywhere in the schema and have no test coverage. Limit #1 (which the pass3 table marks
`n/a`) is modeled by a separate `PerTradeRiskConfig`, not a `LimitRow`, and limit #11
(`count`) is a plain `int` field, not a `LimitRow` either — so in the current schema only
`"gross"`/`"net"` are ever actually reachable. Not a correctness bug (the type is still valid if
a future `LimitRow` needs those modes), just dead surface with no test proving it. —
`tradebot/config/schema.py:143`

**MINOR (advisory, not a defect)** — `pass3-systems.md:432`'s own worked example text says the
binding-order report should print `"binding limits: #2,#4,#6,#8,#9"` at defaults, but the
implementation's `render()` would print `#2,#3,#4,#5,#6` (its `_CEILING_COMPARABLE_LIMITS` set).
The session deliberately excludes #8/#9/#10 with a documented rationale
(`schema.py:343-349`: those rows use gap-stressed/margin/notional bases, not raw risk-at-stop, so
the ceiling-vs-threshold comparison isn't mathematically valid for them) — and includes #3/#5
that the doc's prose omits, which look like an oversight in the doc's own example rather than an
intentional exclusion (nothing in the doc explains why #3/#5 would be excluded from "binding").
The DoD only requires limit #7 be flagged dead (satisfied, see above), so this doesn't block the
session — but a future reader diffing the rendered report against the pass3 prose should know the
prose, not the code, is the stale side. Worth a doc fix in a later pass, not a code fix here. —
`tradebot/config/schema.py:337-357` vs. `docs/trading-bot-brainstorm/brainstorm-v2/pass3-systems.md:432`

No CRITICAL or MAJOR findings. The schema faithfully implements the F-034 re-clamp, F-017
membership rules, and F-036 binding-order semantics from pass3 §2.6/pass1-audit.md, stays
strictly inside the session's declared scope fence, and every numeric default was verified
against the source table rather than assumed correct.

MIG-VERDICT: PASS
