# Session Review — S007 · m0-6-tradebot-ci-skeleton-property · ROUND 3 (final)

**Reviewer stance:** fresh and adversarial; I did not build this work.

> **Supersedes the round-2 review of `66a5d57`** (which returned CHANGES with four
> MINOR findings). This round reviews the full branch `main..HEAD` — `66a5d57`
> (original delivery), `7168175` (round-2 remediation), and **`594c755`** (the fix
> for this round's single MINOR, verified below) — against the approved spec's
> Definition of Done
> (`.mig/state/S007.prompt.md` §4), the design authorities it cites
> (`pass3-systems.md` §1.1/§7.2/§8.6, `pass6-accounting.md` §1.3/§6.3,
> `pass8-synthesis.md` §4.2), the project `CLAUDE.md`, and the three-axis readiness
> inequality.

## Diff under review

```
$ git log --oneline main..HEAD
594c755 docs(S007): reconcile the property-module docstring with the pinned hypothesis decision
7168175 fix(S007): remediation round 2 — align with approved spec + close RS007 findings
66a5d57 chore(S007): snapshot remediation output

net (merge-base 868a7ae..HEAD):
 docs/TRADEBOT_CI.md                    | 120 +++
 docs/session-reviews/RS007.md          |  94 ++   <- round-2 report, superseded here
 pyproject.toml                         |   7 +
 requirements.txt                       |   9 +-
 scripts/run_pr_checks.sh               |  85 ++
 tests/unit/test_tradebot_properties.py | 868 ++++
 tradebot/core/events.py                |  17 +-
```

`.github/workflows/ci.yml` is net-zero across the branch (added in `66a5d57`,
deleted in `7168175`; no `.github/` directory exists in the worktree).

## Evidence base — what I actually ran

Unlike round 2, this round **executed** the verify command. All runs from the S007
worktree, using the pinned interpreter:

1. **Full `VERIFY_CMD`** (`.venv/bin/python -m unittest discover -s tests/unit -p
   'test_*.py'`): **`Ran 612 tests in 1215.3s — OK`, exit code 0** (captured
   explicitly, not inferred from a pipeline). 593 baseline + 19 new property tests.
2. **Property tier alone** (`-m unittest tests.unit.test_tradebot_properties -v`):
   **`Ran 19 tests in 460.4s — OK`.**
3. **`scripts/run_pr_checks.sh` non-zero path, proven not asserted:**
   `TRADEBOT_PY=/bin/false ./scripts/run_pr_checks.sh` → both tiers report FAIL,
   `PR checks FAILED`, **exit code 1**. (The green path of each tier is the exact
   command run in items 1–2; the script's `[ -x "$PY" ]` guard exits 2 with a clear
   message when no venv resolves.)
4. **P4 has teeth (the DoD's red-path demand):** with `EventLog.verify_chain`
   monkeypatched to always return the cached head ("never detects corruption"),
   `TestP4HashChainDetectsAnyMutation` fails **24/24 cases** (`FAILED
   (failures=24)`). The test cannot pass against a lobotomized detector.
5. **Tie-break test is mutation-proven, independently reproduced:** a
   highest-index-tie-break mutant of `round_posting` produces `[4,-3,0,0,-1]` /
   `[-4,5,0,0,-1]` where the test pins `[5,-4,0,0,-1]` / `[-3,4,0,0,-1]` — both
   assertions fail under the mutant. (Note: Python's `max(range(n), key=abs)`
   keeps the *first* tie, so that specific round-2 mutant is behaviorally identical
   to the lowest-index law — not a surviving mutant, just an equivalent one.)
6. **F-038 fix spot-checked live:** `canonical_json({'n':1e-20})` ≠
   `canonical_json({'n':2e-20})`, both exponent-free exact expansions;
   `hypothesis 6.161.8` importable from the pinned venv (inside the `>=6,<7` pin);
   `test_tradebot_properties.py` imports nothing from `hypothesis` (its 4 mentions
   are prose).

## Round-2 findings — closure verified

| # | Round-2 finding | Status | Evidence |
|---|---|---|---|
| 1 | `TestP4PreImageBoundary` pinned 4 of the 6 columns its docstring claims | **CLOSED** | `UNCHAINED` now lists all six (`test_tradebot_properties.py:243-250`), and the six match the design: `pass3-systems.md:30` chains exactly `prev_hash‖seq‖schema‖schema_version‖ts_event‖canonical_json(payload)` |
| 2 | `test_the_law_is_deterministic` could not fail | **CLOSED** | Renamed honestly to `test_the_law_does_not_mutate_its_input` (`:820`, with a docstring admitting exactly what it proves); new `test_ties_for_largest_line_break_to_the_lowest_index` (`:834`) kills the highest-index mutant — reproduced independently, item 5 above |
| 3 | Doc claimed "wired, blocking" CI that nothing runs | **CLOSED** | `ci.yml` deleted; `docs/TRADEBOT_CI.md:21-26` states **"There is no hosted CI"**, defines "blocking" as script-exit-nonzero, and names what a hosted runner waits on; `scripts/run_pr_checks.sh:8-10` repeats it at the point of use; non-zero exit proven (item 3 above) |
| 4 | Unchained `actor` security gap documented but untracked | **CLOSED** | Backlog row `chain-protect-or-explicitly-adr-the` exists (orchestrator tree `docs/sessions/_BACKLOG.md:22`, source `review-carry`, full failure scenario in the note) — filed via the sanctioned channel, not by editing the ledger |

## Definition of Done (spec §4) — walkthrough

| DoD item | Verdict | Evidence |
|---|---|---|
| `hypothesis>=6,<7` pinned in BOTH files | **MET** | `requirements.txt:41-47`, `pyproject.toml` `[project.optional-dependencies] test`; installed 6.161.8 satisfies the pin |
| P4 test exists, uses `@given`, red-proven | **MET with declared divergence** (see below) | `TestP4HashChainDetectsAnyMutation` — seeded-stdlib, not `@given`; red path proven by the lobotomy run (24/24 fail) |
| P6: double-replay + dup-key-drop cases | **MET** | `:424` (twice), `:441` (writer-side dup drop through a real `EventLog`), plus a resume-from-partial-fold case (`:431`) beyond the ask |
| P5: fuzzed interleaved guard/apply | **MET** | `:322` — randomized yield counts vary the interleaving; asserts single winner, counted `race_loser` losers, no raise |
| P7/P8/P11 as explicitly-skipped stubs citing source + milestone | **EXCEEDED, declared divergence** (see below) | Implemented as 10 real tests instead of skips; citations live in the module table (`:1-42`) and per-class docstrings |
| `run_pr_checks.sh` exists outside `tradebot/`, non-zero proven | **MET** | `scripts/run_pr_checks.sh`; exit 1 demonstrated; §8.6 later tiers listed as explicit named SKIPs, not silent omissions |
| No new file under `tradebot/` for CI plumbing | **MET** | `tradebot/` gained no files; only `core/events.py` edited |
| Full suite green, 593+ | **MET** | **612 tests, OK, exit 0** (run by me, not taken from prose) |
| Committed forward-only, scoped by path | **MET with two declared deviations** | Two commits, no history rewrite. Beyond the enumerated paths: `docs/TRADEBOT_CI.md` (documentation of this delivery — round-2 finding 3 *required* editing it), `docs/session-reviews/RS007.md` (the review artifact itself), and `tradebot/core/events.py` (divergence B below) |

## The two declared divergences, judged on the merits

**A. Seeded-stdlib tests instead of `@given`; P7/P8/P11 implemented rather than
stubbed.** **Acceptable.** The DoD's operative intent — a property tier that exists,
bites, and a dependency question settled with the owner — is met and exceeded: the
pin satisfies DoD item 1 to the letter; P4 is proven to fail against a
non-detecting `verify_chain`; P7 found and now guards a real defect; determinism
preserves the flake-free verify gate this repo has already paid one session to
defend. Implementing P7/P8/P11 is strictly more than the "skipped stub" the DoD
asked for, with the P8 `src/` targeting and P11 reference-law status both argued
and fenced (module docstring `:31-41`, `TRADEBOT_CI.md` §2). The trade-off is
recorded in `TRADEBOT_CI.md` §3 with a named cost (no shrinking) and a committed
adoption path (`@given` at M1 for P1). Divergence declared in the commit message,
not smuggled. One residue of this reconciliation was initially left behind — see
the single finding below, since closed by `594c755`.

**B. `tradebot/core/events.py` F-038 fix kept despite the no-tradebot-source scope
line.** **Acceptable.** The old `f"{value:.17f}"` fallback rendered every float
below ~1e-17 as `"0.0"` — a genuine hash collision in the money-truth substrate,
which is precisely the defect class F-038 forbids and M0's acceptance claims to
discharge. The fix is injective (verified live, item 6), regression-guarded
(`test_sub_micro_floats_do_not_collapse_onto_one_string`), and its blast radius is
empty — I verified no committed tradebot event log exists (`data/db/*.db` are v14
state tables: `audit_log`, `active_orders`, `trade_history`) and no tradebot test
pins a 40+ hex-char golden hash. Reverting a verified defect fix to honor a scope
fence would be process over substance; the post-M1 consequence (chain migration,
not edit) is named in `TRADEBOT_CI.md` §2.

## Findings

### MINOR (CLOSED by `594c755`) — the module docstring recorded the *pre-reconciliation* dependency decision, contradicting the branch's own pin

As delivered in `7168175`, `tests/unit/test_tradebot_properties.py:43-58` ("Why
no ``hypothesis``") still said: *"Decision: **stdlib-only** … 'add a dep' is not
a call it may make … revisit it as an ADR, not in passing"* — contradicted by the
same branch's `requirements.txt` pin and by `docs/TRADEBOT_CI.md` §3, which
commit `7168175` itself names as the reconciled decision. The tree carried two
decision records disagreeing on an owner-ratified policy topic
(ask-before-new-deps), in the exact file the M1 property session opens first.

**Closure verified, not trusted.** `594c755` rewrites the block (now "Why this
module does not import ``hypothesis``", `:43-58`): the dependency is stated as
decided and pinned per the approved spec with `TRADEBOT_CI.md` §3 named as the
authoritative record; the seeded-stdlib rationale is kept (seed reproducibility,
no example-DB variance) with the cost still named; `@given` adoption at M1
"starting from the already-pinned version". I verified:

* The diff touches only the docstring — no executable line changed.
* A branch-wide sweep for stale decision text
  (`stdlib-only | not a call it may make | requirements.txt unchanged | as an
  ADR, not in passing | no new dependency` across every file changed on the
  branch) finds only benign hits: `TRADEBOT_CI.md:95` quotes the original backlog
  *question* before answering it, `events.py:28` is about the UUID
  implementation, and this report quotes the old text.
* The edited module still runs: `TestP11PostingBalance` +
  `TestP4PreImageBoundary` at `594c755` → `Ran 7 tests — OK`.

### Notes (not actionable, recorded for the file)

* The exact-`Decimal` expansion makes canonical strings for denormals long (5e-324
  → ~760 chars). Bounded (~1078 digits worst case), reachable only below 1e-4, and
  correctness-first is the right trade for a params-hash substrate. No action.
* `run_pr_checks.sh` runs the property module twice (tier 1's discover includes
  it). Deliberate and commented ("attributable on sight"); costs ~8 min. No action.

## Three-axis readiness

* **Does it work:** yes, observed this round — 612/612 green with exit code
  captured; the property tier green standalone; the script's failure path exits 1;
  P4 proven to fail against a non-detecting chain verifier; the tie-break test
  proven to kill its mutant. The round-2 "argued statically, not observed" caveat
  is discharged.
* **Is it safe:** yes. Dependency addition was spec-approved and pinned (test-extra
  in `pyproject.toml`, documented in `requirements.txt`); the M0 fence holds
  (`grep 'from src|import src' tradebot/` — empty); the one production change's
  blast radius re-verified empty; the unchained-`actor` gap is now tracked in the
  backlog rather than living only in prose.
* **Is it worth it:** yes. Closes the last M0 bullet with a property tier that
  demonstrably bites, and the honest no-hosted-CI framing removes the round-2 risk
  of a gate that exists only on paper.

## Verdict

The four round-2 findings are genuinely closed — each verified by execution or
mutation, not by reading the commit message — and both declared DoD divergences
are acceptable for the reasons above. The single residue this round found (the
module header still asserting the dependency decision that `7168175` reconciled
away) is closed by `594c755`: docstring-only, verified against a branch-wide
sweep for stale decision text, module re-run green. The suite stands at 612/612
green under the exact `VERIFY_CMD`, the property tier demonstrably bites (P4
fails 24/24 against a lobotomized `verify_chain`; the P11 tie-break kills its
mutant), the failure path of `run_pr_checks.sh` exits non-zero, and every doc
claim I checked now matches the tree. No actionable findings remain.

MIG-VERDICT: PASS
