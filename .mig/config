# .mig/config — parsed KEY=VALUE (never sourced); MIG_<KEY> env overrides.
PROJECT_NAME="titan-ict-bot"
VERIFY_CMD="/home/kiyingijmc/projects/Titan_ICT_Bot_v14_3pro/.venv/bin/python -m unittest discover -s tests/unit -p 'test_*.py'"
VERIFY_PATHS="src tests main.py bridge scripts tradebot"
DOMAINS="api data models infra"
RISKY_DOMAINS="execution risk_management order_lifecycle bridge_protocol mql5"
MODEL_BUILD="sonnet"
MODEL_REVIEW="sonnet"
MODEL_STRONG="opus"
# 2026-08-07: suite = 1193 tests / 3042s on an IDLE box (see memory verify-suite-exceeds-mig-timeout);
# 2400 was breached ~27% so every gate rc=124'd on green code. 4800 is a stopgap — the durable fix is
# the parallel-test-runner backlog row; do NOT bump a 4th time.
VERIFY_TIMEOUT="4800"
# Context layer (OPT-IN — uncomment to enable; see assets/context.graphify.example).
# Fail-SOFT: a broken provider warns and the run proceeds hint-less.
#CONTEXT_REFRESH_CMD="graphify update ."
#CONTEXT_ARTIFACT="graphify-out/graph.json"
#CONTEXT_HINT_FILE=".mig/context-hint.md"
#CONTEXT_REFRESH_TIMEOUT="120"
