# titan-ict-bot — independent session review

You did NOT build this. Audit ONLY the committed work on this branch against the
project's authority docs and the session's own Definition of Done. Verify claims
by running the project's verify command where possible.

Report findings by severity (CRITICAL / MAJOR / MINOR), each with file:line evidence.
End the report with EXACTLY ONE line:
  MIG-VERDICT: PASS      (no actionable findings)
  MIG-VERDICT: CHANGES   (findings a remediation pass must address)
